# 손상·복구 기록 (2주차 비교과 과제 ③)

1. **손상**: `out/ring_density.md`에서 R2(500–1000 m) 행을 통째로 삭제하고 R3의 밀도값을 1,299 → 9999로 조작했다 (`sed -i '/^| R2 /d; s/\*\*1,299\*\*/**9999**/'`). `git status`가 ` M out/ring_density.md`로, `git diff`가 1 insertion·2 deletions로 손상을 잡아냈다.
2. **복구**: 직전 커밋 `6b5c67a`의 내용을 `git checkout HEAD -- out/ring_density.md`로 작업트리에 덮어썼다. 복구 후 SHA-256이 손상 전과 동일한 `ae3662b3…4ade1004`로 돌아왔고 `git status`가 비어 변경 없음을 확인했다.
