# 손상·복구 기록 — 최승하 (2주차 비교과 과제 ③)

1. **손상**: `ring_density/README.md` 결과표에서 2–3km 행을 지우고 0–1km 밀도를 1,261 → 9,999로 바꿨고(`sed -i '/^| 2–3km /d; s/^| 0–1km | 1,261 |/| 0–1km | 9,999 |/'`), `git status`가 ` M ring_density/README.md`, `git diff`가 1 insertion·2 deletions로 잡아냈다.
2. **복구**: 직전 커밋 `cea13b3`의 파일을 `git restore --source=HEAD -- ring_density/README.md`로 되살렸고, 줄바꿈을 뺀 SHA-256이 손상 전과 같은 `d5856a08…7239aa8f`, git 객체 해시가 HEAD와 같은 `2d12ab7`로 돌아와 `git diff`가 비었다.
